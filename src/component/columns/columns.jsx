import { addColumns } from "./columns.utils";

export const Columns = () => {
  return (
    <div>
      <button onClick={addColumns}>Add Columns</button>
      <h1>Hello World</h1>
    </div>
  );
};
